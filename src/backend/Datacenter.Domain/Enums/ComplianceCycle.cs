namespace Datacenter.Domain.Enums;

/// <summary>
/// รอบของงานในปฏิทินงาน — กำหนดว่างานประเภทนั้นเกิดขึ้นกี่ครั้งต่อปีบัญชี
/// และ "งวด" ของงานสิ้นสุดเดือนไหน (ใช้เป็นฐานคำนวณวันครบกำหนด)
/// </summary>
public enum ComplianceCycle
{
    /// <summary>ทุกเดือน — 12 งวดต่อปี</summary>
    Monthly = 1,

    /// <summary>ทุกครึ่งปีบัญชี — งวดเดียวต่อปี สิ้นสุดเดือนที่ 6 ของรอบบัญชี (เช่น ภ.ง.ด.51)</summary>
    HalfYear = 2,

    /// <summary>ปีละครั้ง — งวดสิ้นสุดวันสุดท้ายของรอบบัญชี (เช่น ภ.ง.ด.50, งบการเงิน)</summary>
    Yearly = 3,
}
